# RED: stop after the example's result

Date: 2026-09-21. Command: `node --test tests/closer.test.mjs`.

Live feature-leakage on `3345b2b` defined the subject, then dumped a fifth
idea: pipelines, cross-validation, scikit-learn. The unsliced
machine-learning guide included that pipeline example, so the packet
taught the dump. A leakage question now gets only the scaler slice, which
ends on the inflated score. A closer that introduces two new compound
names after the result fails. No phrase list. The tests were not
weakened.
