# RED: doesn't teaches save vs commit

Date: 2026-09-21. Live `/clear-explanations:explain Explain Git.` on `62e8102`
named Git and taught save vs commit as:

> just saving a file in your editor doesn't do it.

`eval:score` failed `commit-not-editor-save` because the criterion wanted
`does not` / `is not` / `isn't`, not `doesn't`. Command:
`node --test tests/native-routing.test.mjs`. The tests were not weakened.
