# RED: native routing and Git misconceptions

Date: 2026-09-21. Command: `node --test tests/native-routing.test.mjs tests/git-guidance.test.mjs tests/eval-corpus.test.mjs`. The new assertions failed before the router, slices, packet hook, and misconception rules existed: Git introductions still loaded branch/merge material, native skill text still told the model to Read `guides/index.json`, and overwrite claims produced `shouldBlock: false`. Those tests remain the acceptance check. Implementation followed; the tests were not weakened.
