# RED: intro packets must not spend a fifth working-memory slot

Date: 2026-09-21. Command: `node --test tests/native-routing.test.mjs`.

Cowan's central store is about four chunks. Before this change, `Explain Git.`
injected a commits slice that also named the staging workflow, commits on a
branch, and local upload. A kitchen-sink question that named every Git feature
could load all five slices. Those tests failed first. The cap lives in
`pickSlices`, not in SKILL.md. The tests were not weakened.
